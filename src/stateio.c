#include <stdio.h>

#include "state.h"
#include "stateio.h"


void State_translate_grid(struct State *state, uint8_t q, uint8_t r) {
}


void State_normalize(struct State *state) {
}


void State_print(const struct State *state, FILE *stream) {
    struct State normalized = *state;
}
